# Implementation Notes

## 2026-08-12: 既存アプリへの統合方式

- 原仕様のNext.js新規構成へ移行せず、既存のVite + Reactアプリへ段階的に統合する。
- 理由: 認証、チーム、練習記録、動画、統計の既存機能を保護し、変更リスクを抑えるため。
- 骨格抽出と指標計算はブラウザ内で行い、外部解析APIへ動画やフレームを送信しない。
- 解析結果は初期版ではIndexedDBへ保存し、チーム共有は単一分析の安定後に別途設計する。
- Phase 0では解析入口、動画検証用ドメイン関数、テスト基盤、説明文書を追加する。

## 2026-08-12: Phase 1 動画設定ウィザード開始

- 端末動画の選択、形式・250MB上限・60秒上限の検証を追加した。
- 1〜12秒の解析範囲、推定30fpsでのコマ送り、クロップ設定を追加した。
- 投球腕、撮影方向、側面時の投球方向、動画種別、鏡像、メモを設定できる。
- 動画はObject URLでプレビューし、画面破棄時に解放する。
- 実fps推定、指紋生成、登録済みSupabase動画からの開始はPhase 1の後続作業とする。

## 2026-08-12: 登録済み動画から解析を開始

- 個人統計ページの各動画へ「フォームを解析」を追加した。
- 動画URL、記録ID、動画番号、練習日、所有者IDを解析設定画面へ引き継ぐ。
- 登録済み動画では再選択を不要とし、解析範囲の設定から開始する。
- Supabase Storageの動画を端末へ読み込んだ後の解析処理はブラウザ内で行う。

## 2026-08-12: Phase 2 骨格抽出の初版

- `@mediapipe/tasks-vision` 1.0.1とPose Landmarker Liteモデルを固定配置した。
- VIDEOモード、最大2人、各confidence 0.50、セグメンテーション無効で初期化する。
- 選択範囲を推定30fpsで解析し、正規化座標とworld座標をフレーム単位で保持する。
- 同期推論による画面停止を軽減するため、3フレームごとにUIへ制御を返す。
- 進捗、キャンセル、再解析、検出フレーム数、骨格オーバーレイを追加した。
- Web Worker化は端末実測後のPhase 2後続作業とする。
