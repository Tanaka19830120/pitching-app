# ピッチング記録アプリ フォーム解析機能統合計画

- 文書種別: 既存アプリ向け実装方針・段階導入計画
- 作成日: 2026-08-12
- 対象アプリ: ソフトボールピッチング記録アプリ
- 参照仕様: `softball_pitching_form_analyzer_spec.md`（Windmill Form Analyzer仕様書）

## 1. 目的

既存のピッチング記録アプリへ、スマートフォンで撮影したウィンドミル投法の動画をブラウザ内で解析する機能を追加する。

既存の認証、チーム、練習記録、動画保存、統計、管理者機能は維持する。アプリ全体を作り直さず、フォーム解析を独立した機能群として段階的に統合する。

本機能はフォームの優劣を一つの点数で判定するものではない。次の確認を支援する。

- どの瞬間から体幹が傾き始めたか
- 頭部や体幹が投球腕側・グラブ側のどちらへ動いたか
- 腕トップ、前足接地、リリース推定点の順序と間隔
- 投球ごとのフォーム差と再現性
- シャドーと実投球で最初に差が生じた部位と時点

## 2. 既存アプリへ統合する基本方針

### 2.1 現在の技術構成を維持する

参照仕様はNext.jsを想定しているが、既存アプリはVite、React、Tailwind CSS、Supabase、Vercelで構成されている。

フォーム解析のためだけにNext.jsへ移行せず、現在のVite + React構成を維持する。解析ロジックはUIから分離し、将来別構成へ移行できるようにする。

### 2.2 既存機能を壊さず追加する

次の既存機能をそのまま利用する。

- Supabase Authによるログイン
- profilesによる選手情報と管理者権限
- pitch_recordsによる練習記録
- Supabase Storageによる動画保存
- 個人統計とチーム統計
- 自分の記録の編集・削除
- 管理者による他メンバーの記録管理

### 2.3 解析は端末内で行う

骨格抽出、イベント推定、角度計算、比較処理は利用者のブラウザ内で実行する。外部の画像解析APIや生成AIへ動画、フレーム画像、骨格座標を送信しない。

動画の扱いは次の2経路とする。

1. 端末から直接選択した動画
   - 解析のためにクラウドへアップロードしない。
   - Object URLを使ってブラウザ内で処理する。
2. 既存記録に登録済みの動画
   - Supabase Storageから端末へ読み込む。
   - 読み込んだ後の解析はブラウザ内で行う。
   - 別の解析サービスへ再送信しない。

既存アプリでは動画自体がSupabase Storageへ保存されるため、「動画が一切外部へ送信されない」という独立版仕様とは異なる。画面上では、登録動画のクラウド保存と端末内解析を区別して説明する。

## 3. 利用者の操作フロー

### 3.1 登録済み動画を解析する

1. 個人統計ページを開く。
2. 動画付き記録の「フォームを解析」を押す。
3. 対象動画を選択する。1記録に複数動画がある場合は解析対象を1本選ぶ。
4. 動画の1〜12秒をトリミングする。
5. 被写体範囲、投球腕、撮影方向、動画種別、鏡像設定を指定する。
6. 「解析開始」を押す。
7. ブラウザ内で骨格抽出と指標計算を行う。
8. 結果画面で骨格、イベント、数値、グラフを確認する。
9. 自動推定が外れたイベントは1フレーム単位で修正する。
10. 結果を端末内へ保存する。

### 3.2 端末動画を解析する

1. フォーム解析画面で「端末から動画を選ぶ」を押す。
2. 以降は登録済み動画と同じ設定・解析手順を使う。
3. 元動画は解析目的ではSupabaseへアップロードしない。
4. 練習記録への添付は、利用者が明示的に選んだ場合だけ既存の動画アップロード機能を利用する。

### 3.3 2投を比較する

1. 保存済み解析AとBを選ぶ。
2. 撮影方向が同じか確認する。
3. モーション開始からリリース推定点までを0〜100%の位相へ正規化する。
4. 並列再生、骨格重ね合わせ、グラフ重ね合わせを切り替える。
5. 最初に差が継続して大きくなった位相、最大差、主要指標差を表示する。

## 4. 画面への統合

### 4.1 個人統計ページ

各動画に次の操作を追加する。

- フォームを解析
- 解析結果を見る
- 比較に追加

解析結果がない動画では「フォームを解析」、解析済み動画では「解析結果を見る」を主操作にする。

### 4.2 フォーム解析ホーム

下部メニューをすぐに5項目へ増やすとスマートフォンで窮屈になるため、初期版ではホームまたは統計ページから遷移させる。

解析機能が安定した後、下部メニューまたは「その他」メニューへ次を追加する。

- 新規解析
- 2投比較
- 保存済み解析
- 撮影のコツ

### 4.3 解析設定画面

- 動画選択
- 1〜12秒のトリミング
- 1フレーム送り・戻し
- 被写体のクロップ範囲
- 右投げ・左投げ
- 正面、背面、投球腕側面、グラブ側面、不明
- 画面上の投球方向
- シャドー、ボールありシャドー、実投球
- 通常映像、鏡像
- 任意メモ

### 4.4 単一分析結果

次の順番で表示する。

1. 解析品質
2. 骨格オーバーレイ付き動画
3. スロー再生、コマ送り、イベント移動
4. 主要イベントのタイムライン
5. イベント修正
6. 最大3件の確認ポイント
7. 主要指標
8. 時系列グラフ
9. 主要フレーム
10. 解析条件と注意事項

## 5. MVPで解析する内容

### 5.1 骨格

MediaPipe Pose Landmarkerを使用し、33ランドマークを抽出する。正式な表示値は単眼動画の2D投影値とする。

### 5.2 主要イベント

- モーション開始
- 腕トップ
- 前足接地
- リリース推定点
- フォロースルー

「リリース」と断定せず、必ず「リリース推定点」と表示する。イベントには信頼度と、自動推定・手動確定の区別を持たせる。

### 5.3 主要指標

- 体幹の左右傾き
- 頭部の横ずれ
- 肩の傾き
- 骨盤の傾き
- 投球腕の肘角度
- 前脚の膝角度
- 投影ストライド長
- 腕トップから前足接地までの時間
- 前足接地からリリース推定点までの時間

値が低信頼の場合は薄色または破線で表示し、通常値と区別する。

### 5.4 表示しないもの

- 技術の絶対的な総合点
- 真の3D関節角度
- ボール離脱時刻の断定
- 医療診断、障害リスク診断
- 理想フォームとの断定的な優劣比較
- 自動球速、回転数、回転軸

## 6. 保存と共有

### 6.1 初期版

解析結果はIndexedDBへ保存し、元動画はIndexedDBへ保存しない。

保存対象:

- 動画の指紋とメタデータ
- 解析設定
- 骨格フレーム
- 主要イベント
- 指標
- 解析品質
- ルールベースの確認ポイント
- メモ
- 各種バージョン

### 6.2 既存記録との関連付け

解析結果には任意で次を保持する。

- `pitchRecordId`
- `videoUrl`または動画を識別する指紋
- `ownerUserId`

Supabaseの動画URLだけを解析結果の恒久的な識別子にしない。動画差し替えや削除に備え、ファイル指紋も使用する。

### 6.3 チーム共有

初期MVPでは骨格データをSupabaseへ保存しない。まず端末内保存で精度、性能、プライバシーを確認する。

将来共有する場合は本人の明示的な許可を必要とし、次のいずれかを選ぶ。

- 主要指標と確認ポイントだけを共有
- 解析JSON全体を共有
- 管理者だけ閲覧可能

共有前にRLS、削除連動、権限変更時の扱いを別途設計する。

## 7. 技術構成

### 7.1 継続利用

- Vite
- React
- Tailwind CSS
- Recharts
- Supabase Auth、Database、Storage
- Vercel

### 7.2 追加候補

- `@mediapipe/tasks-vision`: 33点骨格推定
- `dexie`: IndexedDB管理
- `zod`: 保存・インポートデータ検証
- `vitest`: 純粋関数と解析ロジックの単体テスト
- `@testing-library/react`: 画面テスト
- `playwright`: 主要操作のE2Eテスト

### 7.3 推奨ディレクトリ

```text
src/
  components/
    analyzer/
    video/
    overlay/
    timeline/
    charts/
  pages/
    AnalyzerHome.jsx
    AnalyzerSetup.jsx
    AnalyzerResult.jsx
    AnalyzerCompare.jsx
    AnalyzerGuide.jsx
  domain/
    coordinates/
    filters/
    events/
    metrics/
    comparison/
    feedback/
    quality/
    schemas/
  infrastructure/
    mediapipe/
    video/
    storage/
    export/
  workers/
    pose.worker.js
```

解析式をReactコンポーネントへ直接書かない。`domain`はDOM、React、Supabase、MediaPipe固有型へ依存しない純粋関数を基本とする。

## 8. 実装フェーズ

### Phase 0: 統合準備

- 原仕様をリポジトリへ参照用として保存
- 本統合計画と実装上の判断記録を整備
- テスト基盤を追加
- 解析ページへ遷移できる仮画面を追加
- 既存機能のビルド確認

完了条件:

- 既存機能が引き続き動作する
- lint、test、buildが成功する

### Phase 1: 動画入力と設定

- 登録済み動画から解析を開始
- 端末動画から解析を開始
- 動画形式、250 MB、60秒の検証
- 1〜12秒のトリミング
- コマ送り・戻し
- クロップ
- 投球条件
- 動画指紋
- Object URLの確実な解放

完了条件:

- iPhone由来の対応MOVまたはMP4を設定完了まで進められる
- 非対応形式でアプリが停止せず案内を表示する

### Phase 2: 骨格抽出

- MediaPipeモデルを固定バージョンで配置
- PoseAdapter
- Web Worker
- メインスレッドfallback
- フレーム抽出
- 進捗とキャンセル
- 骨格オーバーレイ

完了条件:

- 短い実動画からPoseFrame配列を取得できる
- 解析キャンセル後に再実行できる

### Phase 3: イベント・指標・品質

- 欠損補間と平滑化
- 座標の正準化
- 主要イベント推定
- 主要指標
- 解析品質
- ルールベースコメント
- 右投げ、左投げ、鏡像の合成テスト

完了条件:

- ドメイン単体テストが成功する
- 左右の符号が投球腕側・グラブ側と一致する

### Phase 4: 単一分析結果

- 動画操作
- 骨格表示切替
- タイムライン
- イベント手動修正
- 指標カード
- グラフ
- 主要フレーム
- 注意表示

完了条件:

- イベント修正後に指標を再計算できる
- 低信頼状態を画面で識別できる

### Phase 5: 2投比較

- 解析A/B選択
- 位相同期
- 並列再生
- 骨格重ね合わせ
- 差分グラフ
- 最初に差が生じた位相
- 再現性指標
- 撮影方向不一致時の制限

### Phase 6: 保存と書き出し

- IndexedDB
- 解析履歴
- JSON、CSV、PNG出力
- 個別削除と全削除
- スキーマバージョンと移行
- 元動画がない場合の再選択案内

### Phase 7: モバイル最適化

- PWA
- モデルキャッシュ
- iPhone・Android動作確認
- メモリ使用量確認
- エラー表示
- アクセシビリティ
- READMEとライセンス

## 9. 初回リリース範囲

最初のリリースは「フォーム解析β版」とし、以下へ限定する。

- 登録済み動画または端末動画を1本選択
- 右投げ・左投げ、撮影方向を指定
- 1〜12秒の範囲指定
- ブラウザ内で33点骨格を抽出
- 骨格オーバーレイ再生
- スロー再生とコマ送り
- 体幹傾き、頭部横ずれ、肘角度、膝角度
- 腕トップ、前足接地、リリース推定点
- イベントの手動修正
- 解析品質表示
- 結果の端末内保存

2投比較、クラウド共有、PNG・CSV出力はβ版の単一分析が安定してから追加する。

## 10. プライバシー表示

画面には次の内容を分かりやすく表示する。

> フォーム解析はこの端末のブラウザ内で行われます。解析のために動画、切り出し画像、骨格データを外部の解析サービスへ送信しません。練習記録へ登録した動画は、チーム共有のためSupabase Storageに保存されます。

## 11. 解析上の注意表示

> この機能は1台のカメラ映像を画面平面上で解析します。表示する角度や距離は2D投影値です。身体のねじれ、カメラ方向への移動、実際のボール離脱時刻を正確に測定できない場合があります。「リリース推定点」はボール離脱そのものを保証する値ではありません。

## 12. 実装開始時の優先事項

1. 現在のアプリとデータを壊さない。
2. 解析のために動画や画像を外部サービスへ送信しない。
3. iPhoneで動画選択、トリミング、解析キャンセルが安定することを優先する。
4. 自動推定精度だけに依存せず、イベントを手動修正できるようにする。
5. 解析ロジックを純粋関数として分離し、テストを用意する。
6. 単一分析が安定するまで比較・共有を先行実装しない。

## 13. 完了判断

各Phaseは、実装だけでなく次を満たした時点で完了とする。

- 正常系と主要な異常系を確認した
- スマートフォン幅で操作できる
- ローディング、キャンセル、エラー、空状態がある
- Object URL、Worker、ImageBitmapを解放する
- 推定値と実測値を混同する文言がない
- lint、test、production buildが成功する
- 既存の記録、統計、チーム、動画機能が壊れていない

